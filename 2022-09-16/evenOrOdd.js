function even_or_odd(number) {
    // (number) => string
    // take integer as an argument, return 'Even' for even numbers
    // and 'Odd' for odd numbers;
    return (number % 2 == 0 ? 'Even' : 'Odd');  
  }