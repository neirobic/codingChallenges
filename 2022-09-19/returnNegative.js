function makeNegative(num) {
    // (number) => number
    // if number < 0, return number. if number > 0, 
    //convert number to negative and return that
    return ((num > 0) ? num * -1 : num) ; 
  }