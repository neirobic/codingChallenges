function opposite(number) {
    //(number) => number
    //given an integer or floating-point number, find the opposite number
    // (1) => -1
    // (14) => -14
    // (-34) => 34
    
    return number * -1;
  }